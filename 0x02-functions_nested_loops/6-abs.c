#include "main.h"

/**
 * _abs -  computes the absolute value of an integer
 *
 * @b: represent number
 *
 * Return: according to code
 */

int _abs(int b)
{
	if (b >= 0)
		return (b);
	else
		return (-b);
}
